var nombre = promt("Escribe tu nombre: ");
var edad = prompt("Escribe tu edad: ");

if (edad >= 18) {
    console.log("Puedes ingresar.");
}

if ((nombre = Mario) || (nombre = Carlos) && edad >= 18) {
        console.log ("Puedes ingresar a la sección VIP.")
}

else {
    console.log ("No puedes ingresar.")
}