// Por medio de un ciclo for, iterar el array creado en el ejercicio 2 e imprimir cada uno de los elementos de manera ordenada.

let arreglo = [];

for(var i = 0; i < 5; i++){
    arreglo.length = (i);
    arreglo.sort();
    console.log(arreglo.length);
}